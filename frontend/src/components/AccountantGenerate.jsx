import React from "react";

const AccountantGenerate = () => {
  return (
    <div style={{ padding: "20px" }}>
      <h2>Generate Tickets</h2>
      <p>Select categories & subcategories to generate tickets and export them as CSV.</p>
      {/* Add ticket generation & CSV export functionality here */}
    </div>
  );
};

export default AccountantGenerate;
